Backend scripts (for Week 3+):
- generate_token.php → retrieves new token for API
- fetch_property.php → uses token to pull updated property data
Currently not used in Week 1–2; static DB (boxgra6_cali) is sufficient.
